# SMTP-почта для приёма заявок.
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
SMTP_TO=seo@allerhand.ru

# Необязательный запасной URL, который принимает POST с данными заявки.
LEAD_WEBHOOK_URL=

# Необязательный секрет. Передаётся как Authorization: Bearer <secret>.
LEAD_WEBHOOK_SECRET=

# Номер счётчика Яндекс Метрики.
NEXT_PUBLIC_YM_ID=
